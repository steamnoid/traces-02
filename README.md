This repository contains Sepolia L1 and Base Sepolia L2 execution traces
and auto-generated diff reports used for client divergence analysis.
Artifacts include diff JSON and separate L1/L2 trace JSON files.

